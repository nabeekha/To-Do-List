{
  "name": "to-do-list",
  "version": "1.0.0",
  "description": "To do list application I built this app to show I could create a functional application in one day, even if it is small. Steal it if you want",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwindcss -i ./input.css -o ./output.css",
    "watch": "tailwindcss -i ./input.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "daisyui": "^4.12.11",
    "tailwindcss": "^3.4.13"
  }
}
